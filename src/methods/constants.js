const SELLING_PRICE = 60;
const ORDER_PRICE = 200;
const PROGRAMM_PRICE = 30;
const MARKETING_PROFIT = 300;

module.exports = Object.freeze({
    SELLING_PRICE, 
    ORDER_PRICE, 
    PROGRAMM_PRICE, 
    MARKETING_PROFIT
});